.row
  .col-xs-10
    app-shopping-edit

hr

.row
  ul.list-group
    a.list-group-item.shopping-list-item(
      *ngFor='let ingredient of ingredients; let i = index',
      (click)="onEditItem(i)"
    ) {{ ingredient.name }} ({{ ingredient.amount }})